{
  "name": "mate",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../.env --env-file=.env --watch src/index.ts",
    "start": "bun --env-file=../.env --env-file=.env src/index.ts",
    "test": "bun test",
    "splice:login": "bun --env-file=../.env --env-file=.env src/cli/splice-login.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@aibleton/protocol": "workspace:*",
    "@anthropic-ai/sdk": "latest",
    "@modelcontextprotocol/sdk": "latest",
    "hono": "latest",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "bun-types": "^1.1.0"
  }
}
